import ContactClient from './ContactClient';

export const metadata = {
  title: 'Portfolio - Contact',
  description: 'Get in touch with me. Feel free to send a message!',
};

export default function Contact() {
  return <ContactClient />;
}